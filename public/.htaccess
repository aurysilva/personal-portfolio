<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Leave WordPress in /backendwp untouched
  RewriteRule ^backendwp(/|$) - [L]

  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
